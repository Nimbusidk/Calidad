# .github/workflows/ci.yml
name: holamundo

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Usar Node.js
        run: echo "¡Hola Mundo!"

      
      - name: instalando
        run: npm install -g html-validator-cli

      - name: validator HTML   
        run: html-validator --file=index.html --verbose

      - name: Validar CSS
        run: css-validator --file=src/App.css --verbose
